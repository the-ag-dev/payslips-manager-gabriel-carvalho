import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'payslips-manager-gabriel-carvalho',
  webDir: 'dist'
};

export default config;
